# Configurazione database SQLite per persistenza dati
DATABASE_TYPE=sqlite
DATABASE_URL=sqlite:///workspaces/TechProManager/server/data.db

# JWT Secret per l'autenticazione
JWT_SECRET=una_stringa_sicura_per_sviluppo

# Porta del server
PORT=3001

# Ambiente
NODE_ENV=development
